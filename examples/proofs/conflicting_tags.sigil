// MUST FAIL: @recover and @error on the same step contradict each other.
schema M { v: Int }
transform f(m: M) -> M {}
transform g(m: M) -> M { m }
process P {
  state n: Int = 0
  on m: M {
    let out = m ~> f @timeout(50.ms) @recover(with: g) @error
    n := n + out.v
  }
}
